version: "3.8"

services:
  api:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./content.json:/app/content.json
      - ./contacts.json:/app/contacts.json
      - ./uploads:/app/uploads
    restart: unless-stopped
